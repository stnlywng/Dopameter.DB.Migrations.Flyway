-- V1__Create_schema.sql

-- If you need to create a schema (or database) first, use:
CREATE SCHEMA IF NOT EXISTS `Dopameter` DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- Switch to the newly created schema
USE `Dopameter`;
